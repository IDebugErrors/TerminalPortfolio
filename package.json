{
  "name": "test"  ,
  "version": "0.1.0",
  "homepage": "https://idebugerrors.github.io/TerminalPortfolio",
  "private": true,
  "dependencies": {
    "draggable_dialog": "^1.0.2",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "4.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "npm run build && gh-pages -d build"
    
  },
  "eslintConfig": {
    "extends": [
      "react-app"
       ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "description": "A portfolio template in ReactJS for developers with terminal style",
  
  "repository": {
    "type": "git",
    "url": "git+https://github.com/IDebugErrors/TerminalPortfolio"
  },
  "author": "Jaypatel",
  
  "bugs": {
    "url": "https://github.com/IDebugErrors"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
